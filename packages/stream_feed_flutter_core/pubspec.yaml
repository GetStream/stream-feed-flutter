name: stream_feed_flutter_core
description: Stream Feed official Flutter SDK Core. Build your own chat experience using Dart and Flutter.
version: 0.0.0
homepage: https://getstream.io/
publish_to: none

environment:
  sdk: '>=2.13.0 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  stream_feed:
    path: ../stream_feed

dev_dependencies:
  mocktail: ^0.1.2
  flutter_test:
    sdk: flutter